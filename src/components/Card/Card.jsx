import styles from "./Card.module.css"

function Card({ imageSrc }) {
    return(
        <div className = {styles.card}>
            {/* <img src="#"/> */}
        </div>
    );
}

export default Card;